import 'package:flutter/material.dart';

import '../../../../template/typhography.dart';

Widget headerPayment() {
  return Column(
    children: [
      Text(
        'Nota',
        style: headerText,
      ),
    ],
  );
}
